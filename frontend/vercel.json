{
    "ignoreCommand": "git diff HEAD^ HEAD --quiet -- ../backend/ ../.github/ && echo 'No frontend changes, skipping build' || exit 1",
    "rewrites": [
        { "source": "/(.*)", "destination": "/" }
    ]
}
